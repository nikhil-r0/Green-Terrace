import 'package:flutter/material.dart';
import 'package:green_terrace/models/community_page.dart';

class Community extends StatelessWidget {
  const Community({super.key});

  @override
  Widget build(BuildContext context) {
  return Scaffold(
      body: CommunityPage(),
    );
  }
}